name: test_routing
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  auto_route: ^11.1.0
  flutter:
    sdk: flutter
  package_a:
    path: ../package_a

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  auto_route_generator: ^10.4.0
  build_runner: ^2.10.5

flutter:
  uses-material-design: true
