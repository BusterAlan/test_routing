import 'package:flutter/material.dart';

class CableView extends StatelessWidget {
  const CableView({super.key});

  @override
  Widget build(BuildContext context) => Center(
    child: Text("Cable view"),
  );
}
