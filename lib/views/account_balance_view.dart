import 'package:flutter/material.dart';

class AccountBalanceView extends StatelessWidget {
  const AccountBalanceView({super.key});

  @override
  Widget build(BuildContext context) => Center(
    child: Text("Account balance view"),
  );
}
