import 'package:flutter/material.dart';

class OnDeviceTrainingView extends StatelessWidget {
  const OnDeviceTrainingView({super.key});

  @override
  Widget build(BuildContext context) => Center(
    child: Text("On device training view"),
  );
}
